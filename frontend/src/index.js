import React from "react";
import ReactDOM from "react-dom/client";

function App() {
  return <h1>預約平台登入/訂閱首頁（可替換為你的登入、訂閱元件）</h1>;
}

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<App />);
